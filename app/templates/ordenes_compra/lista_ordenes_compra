<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Órdenes de Compra</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Órdenes de Compra</h1>
        
        <!-- Mensajes Flash -->
        {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
        <div class="alert alert-dismissible fade show mt-3">
            {% for category, message in messages %}
            <div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">
                {{ message }}
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            {% endfor %}
        </div>
        {% endif %}
        {% endwith %}

        <!-- Formulario para crear nueva orden de compra -->
        <div class="card mb-4">
            <div class="card-header">Crear Nueva Orden de Compra</div>
            <div class="card-body">
                <form method="POST" action="{{ url_for('ordenCompra.get_ordenes_compra') }}">
                    {{ form.hidden_tag() }}
                    <div class="form-group">
                        {{ form.lote.label(class="form-label") }}
                        {{ form.lote(class="form-control") }}
                    </div>
                    <div class="form-group">
                        {{ form.articulo_id.label(class="form-label") }}
                        {{ form.articulo_id(class="form-control") }}
                    </div>
                    <div class="form-group">
                        {{ form.estado_orden_id.label(class="form-label") }}
                        {{ form.estado_orden_id(class="form-control") }}
                    </div>
                    <button type="submit" class="btn btn-primary">Crear</button>
                </form>
            </div>
        </div>

        <!-- Lista de órdenes de compra -->
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Lote</th>
                    <th>Artículo</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                {% for orden in ordenes_compra %}
                <tr>
                    <td>{{ orden.id }}</td>
                    <td>{{ orden.lote }}</td>
                    <td>{{ orden.articulo.nombreArticulo }}</td>
                    <td>{{ orden.estado_orden.nombre }}</td>
                    <td>
                        <a href="{{ url_for('ordenCompra.edit_orden_compra', id=orden.id) }}" class="btn btn-warning btn-sm">Editar</a>
                        <form method="POST" action="{{ url_for('ordenCompra.eliminar_orden_compra', id=orden.id) }}" style="display:inline-block;">
                            <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                        </form>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
