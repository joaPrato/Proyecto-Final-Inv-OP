<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Editar Orden de Compra</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Editar Orden de Compra</h1>
        
        <!-- Mensajes Flash -->
        {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
        <div class="alert alert-dismissible fade show mt-3">
            {% for category, message in messages %}
            <div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">
                {{ message }}
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            {% endfor %}
        </div>
        {% endif %}
        {% endwith %}

        <div class="card">
            <div class="card-header">Editar Orden de Compra</div>
            <div class="card-body">
                <form method="POST" action="{{ url_for('ordenCompra.edit_orden_compra', id=orden.id) }}">
                    {{ form.hidden_tag() }}
                    <div class="form-group">
                        {{ form.lote.label(class="form-label") }}
                        {{ form.lote(class="form-control") }}
                    </div>
                    <div class="form-group">
                        {{ form.modeloOrdenCompra_id.label(class="form-label") }}
                        {{ form.modeloOrdenCompra_id(class="form-control") }}
                    </div>
                    <div class="form-group">
                        {{ form.articulo_id.label(class="form-label") }}
                        {{ form.articulo_id(class="form-control") }}
                    </div>
                    <div class="form-group">
                        {{ form.estado_orden_id.label(class="form-label") }}
                        {{ form.estado_orden_id(class="form-control") }}
                    </div>
                    <button type="submit" class="btn btn-primary">Actualizar</button>
                </form>
            </div>
        </div>

        <a href="{{ url_for('ordenCompra.get_ordenes_compra') }}" class="btn btn-secondary mt-3">Volver</a>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
